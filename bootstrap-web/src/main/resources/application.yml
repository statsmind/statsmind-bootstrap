server:
  port: 8099

spring:
  datasource:
    url: jdbc:h2:mem:test_db;DATABASE_TO_UPPER=false;MODE=MYSQL;DB_CLOSE_ON_EXIT=FALSE;NON_KEYWORDS=USER;
  jpa:
    hibernate:
      ddl-auto: update
  h2:
    console:
      enabled: true
  thymeleaf:
    prefix: classpath:/static/